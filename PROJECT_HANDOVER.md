# 專案交接與進度總結：教會數位選舉系統

這份文件記錄了我們截至目前為止的所有建構想法、邏輯架構與修復紀錄。當您未來重啟這個專案時，可以直接閱讀此文件快速銜接。

## 1. 核心安全架構與權限設計 (RBAC)
系統已經發展出一套非常嚴謹的三層權限架構，這是本專案最有價值且最複雜的部分：

*   **系統超級管理員 (SUPER_ADMIN)**：
    *   擁有全域最高權限，可以看到所有機構與使用者。
    *   有權在 Firebase Auth 中徹底刪除使用者的帳號。
    *   可以指派其他人成為「系統超級管理員」或「單位超級管理員」。
*   **單位超級管理員 (ORG_SUPER_ADMIN)**：
    *   採用「局部視角」設計。他們只能看到自己被授權管轄的教會/機構的人員名單。
    *   無法得知其下屬管理員是否在其他教會擁有權限（畫面上完全隱藏「可管轄機構」與「編輯」按鈕）。
    *   進行「刪除」動作時，後端實作了 **Safe Merging Delete (安全合併刪除)**。系統只會拔除該人員在「該單位超管轄下機構」的權限。若拔除後無其他權限，才降級為 GUEST；絕不會波及該人員在其他教會的權限。
*   **一般單位管理員 (ORG_ADMIN)**：
    *   只能管理該教會的選舉場次、選票發放與計票。
    *   完全無法進入「系統管理員設定」與「機構與單位管理」。

## 2. 解決的關鍵技術痛點 (近期修復紀錄)
*   **非同步渲染崩潰 (Blank Page Crash)**：
    修復了前端在繪製管理員列表時，因過早呼叫 `Firebase Auth` 而導致畫面一片空白的問題。我們改用穩定的全域變數 `currentUserId` 來解決渲染時差。
*   **CSS 權重衝突 (Specificity Issue)**：
    解決了 `.active` (顯示頁面) 被 `.role-super-admin` 的安全隱藏規則壓制的問題。透過引入 `!important`，確保使用者在操作 UI (如點擊側邊欄) 時能正確顯示對應區塊。
*   **資料傳遞遺漏 (Empty String Payload)**：
    修復了全域超級管理員無法將單位的權限設為「無權限」的 Bug。原因是原本的表單過濾了空值 (`""`)，導致後端沒有收到刪除指令。現在空值已被允許傳遞，後端可以精準刪除 (Delete Object Key) 該單位權限。
*   **RWD 手機版體驗優化**：
    比照了首頁的設計，為 `election_admin.html` (選舉管理頁面) 加入了手機版的「三條線 (Hamburger Menu)」按鈕，並實作了點擊選單後自動收合的互動邏輯，大幅改善行動裝置的體驗。

## 3. 資料庫結構現況
*   **users (使用者集合)**：
    儲存使用者的 `role` (全域角色) 與 `org_roles` (機構角色 Map)。例如 `{ "org_id_1": "ORG_SUPER_ADMIN", "org_id_2": "ORG_ADMIN" }`。
*   **organizations (機構集合)**：
    儲存教會的基本資料，作為一切選舉的最高層級容器。
*   **elections (選舉場次)**：
    依附於特定的機構 (org_id)，並包含多個子集合 (如 Rounds, Candidates, Keys)。
*   **Custom Claims (Firebase Auth)**：
    為了確保安全性，使用者的 `role` 與 `org_roles` 都會被同步寫入 Firebase Auth 的 Custom Claims 中，確保即使有人繞過前端，也無法從後端 API 取得越權的資料。

## 4. 未來重啟專案時的建議 (Next Steps)
1.  **測試邊界情況**：雖然目前的權限隔離非常嚴密，建議未來重啟時，多創幾個測試帳號，嘗試互相授予不同機構的權限，確保在極端複雜的交錯授權下，視角依舊乾淨。
2.  **前端錯誤監控**：建議可以導入 Sentry 或 Google Analytics，以便在未來的發佈版本中，如果再次發生像「畫面空白 (Crash)」的非同步問題，開發者能第一時間收到通知與錯誤代碼。
3.  **UI 組件化 (如果考慮重構)**：目前的 JavaScript 寫法較為直接 (Vanilla JS)。若未來系統繼續龐大，可以考慮將 Sidebar、Topbar 等重複性高的程式碼模組化，方便統一維護。

## 結語
目前的系統已經是一個具備「企業級多租戶 (Multi-tenant) 權限隔離」的高安全性應用程式，所有的程式碼都已經安全地 Push 到了 `main` 分支。您可以放心地暫時擱置，未來隨時都能無縫接軌！
